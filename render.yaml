services:
  - type: web
    name: ipl-cricket-showdown
    env: static
    buildCommand: echo "Static site - no build required"
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /\*
        destination: /index.html
